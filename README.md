# An example on how to combine client on angular server on node with nginx on front and docker as a container system

## Update submodules
```
git submodule update --init --recursive
```
## Run:
```
docker-compose up
```
## Stop: 
```
docker-compose down
```
